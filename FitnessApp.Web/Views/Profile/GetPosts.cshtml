@model ICollection<AllPostsViewModel>

@if (!Model.Any())
{
	<h2 class="text-center text-danger">You do not have any programs yet!</h2>
}
else
{
	<div id="programsContainer">
		<div class="row">
			<partial name="_ProgramPartial" model="@Model" />
		</div>
	</div>
}
